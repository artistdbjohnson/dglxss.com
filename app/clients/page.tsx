import { ClientsPortal } from "@/components/clients-portal";

export const metadata = {
  title: "Clients — Go Live | dglxss",
  description:
    "Go live with your new website. Self-host or managed by dglxss.",
};

export default function ClientsPage() {
  return <ClientsPortal />;
}
