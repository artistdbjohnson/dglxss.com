"use client";

import { HeroParticles } from "@/components/hero-particles";

/** Dimmed particle field for about page */
export function AboutParticles() {
  return <HeroParticles density={0.8} dim={0.88} />;
}
