diff --git a/css/app.css b/css/app.css
index 5e4ccf9..1017f96 100644
--- a/css/app.css
+++ b/css/app.css
@@ -66,20 +66,20 @@ button, input, select, textarea { font: inherit; }
 }
 .skip:focus { top: 12px; }
 
+/* Matte plates. Replaces the shared September PWA frost
+   (blur 18px, saturate 1.45, luminosity) so cards are solid. */
 .liquid-glass {
-  background: var(--glass);
-  background-blend-mode: luminosity;
-  backdrop-filter: blur(18px) saturate(1.45);
-  -webkit-backdrop-filter: blur(18px) saturate(1.45);
-  border: 1px solid var(--glass-border);
-  box-shadow:
-    inset 0 1px 0 rgba(255, 255, 255, 0.42),
-    0 12px 36px rgba(10, 8, 4, 0.1);
+  background: #fff;
+  border: 1px solid var(--line);
+  box-shadow: none;
+}
+html[data-theme="dark"] .liquid-glass {
+  background: #1a160f;
 }
 
 .nav.liquid-glass {
-  --glass: rgba(12, 10, 6, 0.34);
-  --glass-border: rgba(255, 255, 255, 0.18);
+  background: #111111;
+  border-color: rgba(255, 255, 255, 0.16);
   color: #fff;
 }
 
@@ -327,8 +327,8 @@ h1 {
   color: #fff;
 }
 .trust-row .liquid-glass {
-  --glass: rgba(255, 255, 255, 0.12);
-  --glass-border: rgba(255, 255, 255, 0.22);
+  background: rgba(12, 10, 6, 0.92);
+  border-color: rgba(255, 255, 255, 0.18);
 }
 .trust-row b {
   display: block;
@@ -385,7 +385,7 @@ html[data-theme="light"] .card,
 html[data-theme="light"] .stat,
 html[data-theme="light"] .panel,
 html[data-theme="light"] .step {
-  background: rgba(255, 255, 255, 0.36);
+  background: #fff;
 }
 .card:hover { transform: translateY(-2px); }
 .card h3,
@@ -597,8 +597,8 @@ input, select, textarea {
 
 .dock { display: none; }
 .dock.liquid-glass {
-  --glass: rgba(12, 10, 6, 0.58);
-  --glass-border: rgba(255, 255, 255, 0.16);
+  background: #111111;
+  border-color: rgba(255, 255, 255, 0.16);
 }
 
 .band { position: relative; }
@@ -642,10 +642,8 @@ html[data-theme="dark"] .band::before {
     flex-direction: column;
     align-items: stretch;
     gap: 12px;
-    background: rgba(12, 10, 6, 0.78);
+    background: #111111;
     border: 1px solid rgba(255, 255, 255, 0.14);
-    backdrop-filter: blur(18px) saturate(1.4);
-    -webkit-backdrop-filter: blur(18px) saturate(1.4);
   }
   .nav-links.is-open { display: flex; }
   .nav-tools { justify-content: space-between; }
